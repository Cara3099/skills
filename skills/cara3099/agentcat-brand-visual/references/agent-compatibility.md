# 智能体兼容性

- 支持读取 `SKILL.md` 的智能体应先读取模板与参考文件，再执行任务。
- 能编辑 Office 文件的智能体直接复制模板并修改继承对象。
- 只能运行脚本的智能体使用 `scripts/` 下生成器创建稳定起点。
- 无法编辑 PPT 模板时，返回模板文件并说明需要用户在 PowerPoint/WPS 中替换内容，不得从空白页生成低质量替代版本。
- 无法使用原始 Logo 时，保留无 Logo 的文字版式，不重绘或凭提示词生成 Logo。
